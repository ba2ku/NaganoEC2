<div class="container">
    <div class="row">
        <div class="col-1"></div>
        <div class="col-10">
            <br>
            <br>
            <br>
            <div class="">
            </div>
            <h3> <%= @user.kanji_last %> <%= @user.kanji_first %> 様の MyPage</h3>
            <p>※パスワードは定期的に更新されることをおすすめします。</p>
            <br>
            <div class="border-bottom"></div>
            <br>
            <h3>Infomation</h3>
            <p>カスタマーデスクからのお知らせ。</p>
            <div class="notice_user"><%= @user.notice %></div>
            <div class="border-bottom"></div>
            <br>

            <h3>発送情報</h3>
            <p>購入された商品の発送状況</p>
            <% @shopping_history.each do |sh| %>
            <% sh.address_histories.each do |ah| %>
            <div class="item">
                <%= sh.shopping_date.strftime('%Y年%m月%d日 %H:%M') %>
                <%= ah.postcode %> <%= ah.prefecture %> <%= ah.city %> <%= ah.street %> <%= ah.building %>
            </div>
            <% end %>
            <% end %>

            <% @shopping_history.each do |sh| %>
            <% sh.ordered_items.each do |ah| %>
            <div class="item">
                <%= ah.price %>
            </div>
            <% end %>
            <% end %>

            <div class="border-bottom"></div>
            <div class="mypage_field3 padding_top10px margin_top padding_left10px">
                <%= link_to "購入履歴", shopping_hists_path(current_user.id) %></div>
            <div class="mypage_field3 padding_top10px margin_top padding_left10px">
                <%= link_to "お客様基本情報確認", user_path(current_user.id) %></div>
            <div class="mypage_field3 padding_top10px margin_top padding_left10px">
                <%= link_to "退会", edit_user_registration_path %></div>
        </div>
        <div class="col-1"></div>


    </div>

</div>